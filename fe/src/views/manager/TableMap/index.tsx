import React from "react";
import { WaiterTableMap } from "../../waiter/tables/index";

export const TableMapIndex: React.FC = () => {
  return <WaiterTableMap isManager={true} />;
};

export default TableMapIndex;
